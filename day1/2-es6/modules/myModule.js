export const topla = (x, y) => x + y;
export const cikar = (x, y) => x - y;
export const carp = (x, y) => x * y;
export const bol = (x, y) => x / y;

export default topla;

if (condition) {
}

if (condition) {
}

const arr = [ 1, 2, 3, 4 ]
